// Config.xcconfig
// Default values for open-source builds.
// Override by creating Secrets.xcconfig (gitignored) with real API keys.

DEEPGRAM_API_KEY = YOUR_DEEPGRAM_API_KEY
META_APP_ID = 0
META_CLIENT_TOKEN =

// Import local secrets if available (silently ignored when missing)
#include? "Secrets.xcconfig"
